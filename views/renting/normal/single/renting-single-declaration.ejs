<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Renting Single Owner Declaration Letter</title>
    <style>
        body {
            background-color: #f0f0f0;
            font-family: "Times New Roman", Times, serif;
            display: flex;
            justify-content: center;
            padding: 20px;
            margin: 0;
        }

        /* Main Page Sheet */
        .page {
            background-color: white;
            width: 210mm;
            /* A4 width */
            min-height: 297mm;
            /* A4 height */
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            position: relative;
            box-sizing: border-box;
            padding: 15px;
        }

        /* Decorative Border Generation using CSS Gradients */
        .border-frame {
            width: 100%;
            height: 100%;
            box-sizing: border-box;
            border: 2px dotted #333;
            padding: 4px;
            position: relative;
            display: flex;
            flex-direction: column;
        }

        /* Inner pattern to simulate the Greek Key border */
        .pattern-border {
            flex-grow: 1;
            border: 1px double #333;
            padding: 30px 40px;
            position: relative;
        }

        /* Simulating the meander pattern using a repeated outline effect */
        .page::before {
            content: "";
            position: absolute;
            top: 5px;
            left: 5px;
            right: 5px;
            bottom: 5px;
            border: 2px solid #555;
            pointer-events: none;
        }

        .page::after {
            content: "";
            position: absolute;
            top: 10px;
            left: 10px;
            right: 10px;
            bottom: 10px;
            border: 4px double #555;
            pointer-events: none;
        }

        /* Content Styling */
        .content-area {
            font-size: 11pt;
            line-height: 1.4;
            color: #000;
        }

        .header-meta {
            margin-bottom: 20px;
            font-weight: bold;
            font-size: 10pt;
        }

        .subject {
            margin-bottom: 20px;
            font-weight: bold;
            text-align: justify;
        }

        .salutation {
            margin-bottom: 15px;
        }

        p {
            margin-bottom: 15px;
            text-align: justify;
        }

        /* Table/List Styling */
        .doc-list {
            margin: 15px 0 25px 0;
            width: 100%;
            border-collapse: collapse;
        }

        .doc-list td {
            padding: 2px 0;
            vertical-align: top;
        }

        .doc-list td:first-child {
            width: 45%;
            font-weight: normal;
        }

        .doc-list-header {
            font-weight: bold;
            margin-bottom: 5px;
            display: flex;
        }

        .doc-list-header span {
            display: inline-block;
        }

        /* Typography Utilities */
        .bold {
            font-weight: bold;
        }

        .section-break {
            margin-top: 25px;
        }

        .signature-block {
            margin-top: 10px;
            margin-bottom: 25px;
        }

        .sig-name {
            font-weight: bold;
            text-transform: uppercase;
            display: block;
        }

        .sig-phone {
            font-weight: bold;
            display: block;
            margin-bottom: 5px;
        }

        .sig-title {
            font-weight: bold;
            font-size: 10pt;
        }

        /* Responsive tweak for small screens */
        @media (max-width: 850px) {
            .page {
                width: 100%;
                height: auto;
            }

            .pattern-border {
                padding: 20px;
            }
        }
    </style>
</head>

<body>

    <div class="page">
        <div class="border-frame">
            <div class="pattern-border">

                <div class="content-area">
                    <!-- Date -->
                    <div class="header-meta">
                        Date: <%= data._current_date %>
                    </div>

                    <!-- Subject -->
                    <div class="subject">
                        Subject: Declaration Letter for <%= data._application_type %>, Application
                            Number: <%= data._application_number %>
                    </div>

                    <!-- Salutation -->
                    <div class="salutation">
                        Respected Sir,
                    </div>

                    <!-- Para 1 -->
                    <p>
                        I am authorized representative of Company <%= data?._company_name %>, hereby submits the
                            following documents (as indicated by UDIN numbers ) for <%= data._application_type %>
                    </p>

                    <!-- Document Table -->
                    <div style="margin-left: 30px; margin-right: 10px;">
                        <div class="doc-list-header">
                            <span style="width: 45%; text-align: left;">Document Type</span>
                            <span style="width: 55%; text-align: left;">Udin Number</span>
                        </div>
                        <table class="doc-list">
                            <% if (data?._documents && data?._documents.length> 0) { %>
                                <% data?._documents.forEach(function(doc) { %>
                                    <tr>
                                        <td>
                                            <%= doc._name %>
                                        </td>
                                        <td>
                                            <%= doc._udin %>
                                        </td>
                                    </tr>
                                    <% }); %>
                                        <% } else { %>
                                            <tr>
                                                <td colspan="2">No documents listed.</td>
                                            </tr>
                                            <% } %>
                        </table>
                    </div>

                    <!-- Para 2 -->
                    <p>
                        On behalf as duly authorised & having competence to do so on behalf of <%= data._company_name %>
                            , I
                            declare that the <%= data._tenant_name %> having <%= data._tenant_id %>
                                    has agreed to conduct activity covered under IT &ITes activities as notified by IT&E
                                    dept. vide
                                    notification 845-IT/O/117/2013 dated 12.7.2023 (UDIN: <span class="bold">
                                        <%= data._it_notification_udin %>
                                    </span>) . I shall indemnify and hold the state
                                    harmless, including all associated costs in case of any miss-representation. The
                                    data provided
                                    by me including total area of building <%= data._building_area_sqft %> (sqft) and
                                        total
                                        commercial area <%= data._commercial_area_sqft %> (sqft) is
                                            true to best of my knowledge.
                    </p>

                    <!-- Legal Warning 1 -->
                    <p>
                        I also understand that any kind of miss-representation will invite legal action as per law.
                    </p>

                    <!-- Signature 1 (Company Rep) -->
                    <div class="signature-block">
                        <span class="sig-name">
                            <%= data._company_rep_name %>
                        </span>
                        <span class="sig-phone">
                            <%= data._company_rep_phone %>
                        </span>
                        <span class="sig-title">(Signature of authorized representative)</span>
                    </div>

                    <!-- Para 3 -->
                    <p>
                        On behalf as duly authorised & having competence to do so on behalf of <%= data._tenant_name %>
                            having <%= data._tenant_id %> declare that the <%= data._sublet_area_sqft %> (sqft) space is
                                    being rented
                                    for IT&ITes Activity as permitted vide notification
                                    1967/UD/O/M/SL(AL/NR)/7L-23/95(Pt.) dated
                                    3rd June 2008 (UDIN: <span class="bold">
                                        <%= data._tenant_notification_udin %>
                                    </span>) . I shall
                                    indemnify and hold the state harmless, including all associated costs in case of any
                                    miss-representation.
                    </p>

                    <!-- Legal Warning 2 -->
                    <p>
                        I also understand that any kind of miss- representation will invite legal action as per law.
                    </p>

                    <!-- Signature 2 (Tenant Rep) -->
                    <div class="signature-block">
                        <span class="sig-name">
                            <%= data._tenant_rep_name %>
                        </span>
                        <span class="sig-phone">
                            <%= data._tenant_rep_phone %>
                        </span>
                        <span class="sig-title">(Signature of authorized representative of Tenant)</span>
                    </div>

                    <!-- Para 4 -->
                    <p>
                        I am Company Secretary having <span class="bold">registration number <%= data._cs_reg_number %>
                        </span>
                        have gone
                        through the project report as submitted by authorized representative of the <span class="bold">
                            <%= data._company_name %>
                        </span> and concur from his views that this
                        project report is covered under IT & ITeS activities as notified by IT&E dept. vide notification
                        845-IT/O/117/2013 dated 12.7.23 (UDIN: <span class="bold">
                            <%= data._it_notification_udin %>
                        </span>) .
                    </p>

                    <!-- Legal Warning 3 -->
                    <p>
                        I also understand that any kind of miss-representation will invite legal action as per law.
                    </p>

                    <!-- Signature 3 (CS) -->
                    <div class="signature-block">
                        <span class="sig-name">
                            <%= data._cs_name %>
                        </span>
                        <span class="sig-phone">
                            <%= data._cs_phone %>
                        </span>
                        <span class="sig-title">(Signature of Company Secretary)</span>
                    </div>

                </div>
            </div>
        </div>
    </div>

</body>

</html>