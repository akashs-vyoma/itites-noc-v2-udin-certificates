<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Declaration Letter - Renting Renewal Multi Party</title>
    <style>
        body {
            background-color: #f0f0f0;
            font-family: "Times New Roman", Times, serif;
            display: flex;
            justify-content: center;
            padding: 20px;
            margin: 0;
        }

        .page {
            background-color: white;
            width: 210mm;
            min-height: 297mm;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            position: relative;
            box-sizing: border-box;
            padding: 15px;
        }

        .border-frame {
            width: 100%;
            height: 100%;
            box-sizing: border-box;
            border: 2px dotted #333;
            padding: 4px;
            position: relative;
            display: flex;
            flex-direction: column;
        }

        .pattern-border {
            flex-grow: 1;
            border: 1px double #333;
            padding: 30px 40px;
            position: relative;
            z-index: 1;
        }

        /* Standard Frame lines */
        .page::before {
            content: "";
            position: absolute;
            top: 5px;
            left: 5px;
            right: 5px;
            bottom: 5px;
            border: 2px solid #555;
            pointer-events: none;
        }

        .page::after {
            content: "";
            position: absolute;
            top: 10px;
            left: 10px;
            right: 10px;
            bottom: 10px;
            border: 4px double #555;
            pointer-events: none;
        }

        /* Standard Watermark */
        .watermark {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            width: 80%;
            opacity: 0.1;
            pointer-events: none;
            z-index: 0;
            text-align: center;
        }

        .watermark img {
            width: 100%;
            height: auto;
        }

        .content-area {
            font-size: 10.5pt;
            line-height: 1.4;
            color: #000;
            position: relative;
            z-index: 2;
        }

        /* Standard Logo Section */
        .logo-container {
            text-align: center;
            margin-bottom: 20px;
        }

        .header-logo {
            height: 80px;
            width: auto;
        }

        .header-meta {
            margin-bottom: 15px;
            font-weight: bold;
            font-size: 10pt;
        }

        .subject {
            margin-bottom: 20px;
            font-weight: bold;
            text-align: justify;
        }

        .salutation {
            margin-bottom: 15px;
            font-weight: bold;
        }

        p {
            margin-bottom: 12px;
            text-align: justify;
        }

        /* Table Styling */
        .doc-list {
            margin: 10px 0 20px 30px;
            width: 90%;
            border-collapse: collapse;
        }

        .doc-list td {
            padding: 3px 10px 3px 0;
            vertical-align: top;
        }

        .doc-list-header {
            font-weight: bold;
        }

        .bold {
            font-weight: bold;
        }

        .signature-block {
            margin-top: 15px;
            margin-bottom: 20px;
        }

        .sig-name {
            font-weight: bold;
            text-transform: uppercase;
            display: block;
        }

        .sig-phone {
            font-weight: bold;
            display: block;
        }

        .sig-title {
            font-weight: bold;
            font-size: 9.5pt;
        }

        @media (max-width: 850px) {
            .page {
                width: 100%;
                height: auto;
            }

            .pattern-border {
                padding: 20px;
            }
        }
    </style>
</head>

<body>

    <div class="page">
        <div class="border-frame">
            <div class="pattern-border">

                <!-- Standard Watermark -->
                <div class="watermark">
                    <img src="images/webel.png" alt="Watermark">
                </div>

                <div class="content-area">

                    <!-- Standard Header Logo -->
                    <div class="logo-container">
                        <img src="images/webel.png" alt="Webel Logo" class="header-logo">
                    </div>

                    <!-- Date -->
                    <div class="header-meta">
                        Date: <%= data?._current_date %>
                    </div>

                    <!-- Subject -->
                    <div class="subject">
                        Subject: Declaration Letter for <%= data?._application_type %>, Application Number: <%=
                                data?._application_number %>
                    </div>

                    <div class="salutation">
                        Respected Sir,
                    </div>

                    <p>
                        I, <span class="bold">
                            <%= data?._rep_name %>
                        </span>, hereby submits the following documents (as indicated by UDIN numbers) for NOC for
                        Renting Out Leased property –
                    </p>

                    <!-- Document Table -->
                    <table class="doc-list">
                        <thead>
                            <tr>
                                <td class="doc-list-header" style="width: 50%;">Document Type</td>
                                <td class="doc-list-header">Udin Number</td>
                            </tr>
                        </thead>
                        <tbody>
                            <% if (data?._documents && data?._documents.length> 0) { %>
                                <% data?._documents.forEach(function(doc) { %>
                                    <tr>
                                        <td>
                                            <%= doc?._name %>
                                        </td>
                                        <td>
                                            <%= doc?._udin %>
                                        </td>
                                    </tr>
                                    <% }); %>
                                        <% } %>
                        </tbody>
                    </table>

                    <!-- Applicant Declaration -->
                    <p>
                        I declare that the <span class="bold">
                            <%= data?._tenant_name %>
                        </span> having <%= data?._tenant_id %> has agreed to conduct <span class="bold">
                                <%= data?._activity_type %>
                            </span> and this space is within the limit of <span class="bold">20%</span> of the total
                            space allowed for <span class="bold">Non IT&ITes Activity</span> vide notification
                            1967/UD/O/M/SL(AL/NR)/7L-23/95(Pt.) dated 3rd June 2008 (UDIN: <span class="bold">
                                <%= data?._notification_udin %>
                            </span>). I shall indemnify and hold the state harmless, including all associated costs in
                            case of any miss-representation.
                            <br>
                            The data provided by me including total area of building <%= data?._building_area_sqft %>
                                (sqft) and total commercial area <%= data?._commercial_area_sqft %> (sqft) is true to
                                    best of my knowledge.
                    </p>

                    <p>I also understand that any kind of miss-representation will invite legal action as per law.</p>

                    <!-- Signature 1 (Applicant) -->
                    <div class="signature-block">
                        <span class="sig-name">
                            <%= data?._rep_name %>
                        </span>
                        <span class="sig-phone">
                            <%= data?._rep_phone %>
                        </span>
                        <span class="sig-title">(signature of authorized representative)</span>
                    </div>

                    <!-- Tenant Declaration -->
                    <p>
                        On behalf as duly authorised & having competence to do so on behalf of <span class="bold">
                            <%= data?._tenant_name %>
                        </span> having <%= data?._tenant_id %> declare that the <%= data?._sublet_area_sqft %> (sqft)
                                space is being rented for <span class="bold">
                                    <%= data?._activity_type %>
                                </span> as permitted vide notification 1967/UD/O/M/SL(AL/NR)/7L-23/95(Pt.) dated 3rd
                                June 2008 (UDIN: <span class="bold">
                                    <%= data?._notification_udin %>
                                </span>). I shall indemnify and hold the state harmless, including all associated costs
                                in case of any miss-representation.
                    </p>

                    <p>I also understand that any kind of miss-representation will invite legal action as per law.</p>

                    <!-- Signature 2 (Tenant) -->
                    <div class="signature-block">
                        <span class="sig-name">
                            <%= data?._tenant_rep_name %>
                        </span>
                        <span class="sig-phone">
                            <%= data?._tenant_rep_phone %>
                        </span>
                        <span class="sig-title">(Signature of authorized representative of Tenant)</span>
                    </div>

                </div>
            </div>
        </div>
    </div>

</body>

</html>